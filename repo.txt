select t.title_name, a.author, y.year, c.category from title t join category c on c.category_id = t.category_id and c.category_id=Comedy join year y on y.title_id = t.title_id join author a on a.author_id = t.author_id;select t.title_name, a.author, y.year, c.category from title t join category c on c.category_id = t.category_id and c.category_id=Thriller join year y on y.title_id = t.title_id join author a on a.author_id = t.author_id;select t.title_name, a.author, y.year, c.category from title t join category c on c.category_id = t.category_id and c.category_id=Bollywood join year y on y.title_id = t.title_id join author a on a.author_id = t.author_id;